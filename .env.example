# ===========================================
# Lumen Backend Environment Configuration
# ===========================================
# Copy this file to .env and fill in the values

# Environment (development, production)
ENV=development

# ===========================================
# DATABASE CONNECTION (Primary - Use This!)
# ===========================================
# PostgreSQL connection URL (preferred method)
# Format: postgres://USER:PASSWORD@HOST:PORT/DATABASE?sslmode=disable
DATABASE_URL=postgres://postgres:your_secure_password_here@localhost:5432/lumen_db?sslmode=disable

# ===========================================
# DATABASE CREDENTIALS (For Docker Compose)
# ===========================================
# These are used by docker-compose.yaml to configure PostgreSQL container
DB_USER=postgres
DB_PASSWORD=your_secure_password_here
DB_NAME=lumen_db

# ===========================================
# OPTIONAL SERVICES
# ===========================================
# Redis Configuration (for caching - future use)
REDIS_URL=redis://localhost:6379

# Server Configuration
PORT=8080
