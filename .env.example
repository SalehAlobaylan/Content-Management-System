# ===========================================
# Lumen Backend Environment Configuration
# ===========================================
# Copy this file to .env and fill in the values

# Environment (development, production)
ENV=development

# ===========================================
# DATABASE CONNECTION (Required)
# ===========================================
# PostgreSQL connection URL - This is the ONLY supported database configuration method
# Format: postgres://USER:PASSWORD@HOST:PORT/DATABASE?sslmode=disable
# Example for local development:
DATABASE_URL=postgres://postgres:your_secure_password_here@localhost:5432/lumen_db?sslmode=disable
# Example for Docker (using service name as host):
# DATABASE_URL=postgres://postgres:your_secure_password_here@db:5432/lumen_db?sslmode=disable

# ===========================================
# OPTIONAL SERVICES
# ===========================================
# Redis Configuration (for caching - future use)
REDIS_URL=redis://localhost:6379

# Server Configuration
PORT=8080
